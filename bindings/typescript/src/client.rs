//! Client interface for rmcp_typescript binding.
//!
//! This module provides the client-side entry point for interacting with the RMCP SDK from TypeScript/JavaScript.
//! It is intended to be used with the `napi` bindings for Node.js interoperability.
//!
//! # Example (TypeScript)
//!
//! ```typescript
//! import { Client } from 'rmcp-typescript';
//! const client = new Client();
//! // ...
//! ```
// Add #[napi] wrappers as needed for TS/JS
